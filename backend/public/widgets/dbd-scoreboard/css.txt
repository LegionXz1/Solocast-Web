@import url('https://fonts.googleapis.com/css2?family=Oswald:wght@600;700&family=Roboto:wght@400;500;700&display=swap');

:root {
    --widget-scale: 1;
    --title-color: #ffffff;
    --sub-color: #aaaaaa;
    --stat-title-color: #aaaaaa;
    --stat-num-color: #ffffff;
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    user-select: none;
}

body {
    background: transparent !important;
    overflow: hidden;
    font-family: 'Roboto', sans-serif;
    color: #ffffff;
}

#main-container {
    position: relative;
    padding: 0;
    display: inline-block;
    background: transparent !important;
    transform: scale(var(--widget-scale, 1));
    transform-origin: top left;
}

.dbd-card {
    width: auto;
    min-width: 480px;
    background: transparent !important;
    border: none !important;
    border-radius: 0;
    padding: 0;
    box-shadow: none !important;
}

.card-header {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-bottom: 20px;
}

.avatar-container {
    width: 84px;
    height: 84px;
    background: transparent;
    border: none !important;
    border-radius: 4px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    filter: drop-shadow(0 3px 6px rgba(0, 0, 0, 0.85));
}

.stickman-avatar {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.header-text-box {
    display: flex;
    flex-direction: column;
}

.role-title {
    font-family: 'Oswald', sans-serif;
    font-size: 44px;
    font-weight: 700;
    line-height: 1;
    letter-spacing: 2.5px;
    color: var(--title-color);
    text-transform: uppercase;
    text-shadow:
        -1.5px -1.5px 0 #000000,
         1.5px -1.5px 0 #000000,
        -1.5px  1.5px 0 #000000,
         1.5px  1.5px 0 #000000,
         0 2px 6px rgba(0, 0, 0, 0.9),
         0 4px 12px rgba(0, 0, 0, 0.7);
    paint-order: stroke fill;
    -webkit-text-stroke: 1.5px rgba(0, 0, 0, 0.85);
}

.cmd-sub {
    font-size: 16px;
    color: var(--sub-color);
    margin-top: 4px;
    text-shadow:
        -1px -1px 0 #000000,
         1px -1px 0 #000000,
        -1px  1px 0 #000000,
         1px  1px 0 #000000,
         0 2px 4px rgba(0, 0, 0, 0.8);
    paint-order: stroke fill;
    -webkit-text-stroke: 1px rgba(0, 0, 0, 0.75);
}

.stats-container {
    display: flex;
    gap: 36px;
}

.stat-box {
    flex: 1;
    background: transparent !important;
    border: none !important;
    border-radius: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    box-shadow: none !important;
}

.stat-title {
    font-size: 16px;
    font-weight: 700;
    color: var(--stat-title-color);
    letter-spacing: 1.5px;
    text-transform: uppercase;
    text-shadow:
        -1px -1px 0 #000000,
         1px -1px 0 #000000,
        -1px  1px 0 #000000,
         1px  1px 0 #000000,
         0 2px 4px rgba(0, 0, 0, 0.8);
    paint-order: stroke fill;
    -webkit-text-stroke: 1px rgba(0, 0, 0, 0.75);
}

.stat-number {
    font-family: 'Oswald', sans-serif;
    font-size: 72px;
    font-weight: 700;
    line-height: 1;
    color: var(--stat-num-color);
    margin-top: 4px;
    transition: transform 0.15s ease;
    text-shadow:
        -2px -2px 0 #000000,
         2px -2px 0 #000000,
        -2px  2px 0 #000000,
         2px  2px 0 #000000,
         0 3px 8px rgba(0, 0, 0, 0.9),
         0 6px 16px rgba(0, 0, 0, 0.75);
    paint-order: stroke fill;
    -webkit-text-stroke: 2px rgba(0, 0, 0, 0.9);
}

/* Optional Toggle to Disable Shadow/Outline */
.no-shadow .role-title,
.no-shadow .cmd-sub,
.no-shadow .stat-title,
.no-shadow .stat-number {
    text-shadow: none !important;
    -webkit-text-stroke: 0 !important;
}

.no-shadow .avatar-container {
    filter: none !important;
}

.stat-number.pop-anim {
    animation: pop-bounce 0.3s ease-out;
}

@keyframes pop-bounce {
    0% { transform: scale(1); }
    50% { transform: scale(1.2); }
    100% { transform: scale(1); }
}
