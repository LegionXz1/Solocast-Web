@import url('https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&family=JetBrains+Mono:wght@500;600&display=swap');

:root {
  --accent-color: #1DB954;
  --accent-glow: rgba(29, 185, 84, 0.45);
  --bg-surface: rgba(18, 18, 22, 0.85);
  --border-color: rgba(255, 255, 255, 0.12);
  --text-primary: #FFFFFF;
  --text-secondary: rgba(255, 255, 255, 0.7);
  --radius-card: 16px;
  --radius-art: 12px;
  --font-main: 'Outfit', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  background: transparent;
  overflow: hidden;
  font-family: var(--font-main);
  color: var(--text-primary);
  width: 100vw;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: flex-start;
  padding: 24px;
}

/* Card Root Container */
.spotify-card {
  position: relative;
  width: 520px;
  min-height: 120px;
  border-radius: var(--radius-card);
  padding: 16px;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  overflow: hidden;
}

/* Theme 1: Glassmorphism (Default) */
.theme-glassmorphism {
  background: linear-gradient(135deg, rgba(26, 26, 36, 0.82), rgba(16, 16, 22, 0.94));
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--border-color);
  box-shadow: 0 12px 36px rgba(0, 0, 0, 0.45), 0 0 24px var(--accent-glow);
}

/* Theme 2: Cyberpunk Neon */
.theme-cyberpunk {
  background: rgba(10, 10, 15, 0.95);
  border: 2px solid var(--accent-color);
  box-shadow: 0 0 20px var(--accent-glow), inset 0 0 15px rgba(0, 0, 0, 0.8);
  border-radius: 6px;
}
.theme-cyberpunk .album-art {
  border-radius: 4px;
}
.theme-cyberpunk .status-pill {
  border-radius: 4px;
}

/* Theme 3: Clean Minimalist */
.theme-minimal {
  background: #111116;
  border: 1px solid rgba(255, 255, 255, 0.08);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
  border-radius: 12px;
}
.theme-minimal .spotify-card {
  box-shadow: none;
}

/* Theme 4: Retro Vinyl */
.theme-vinyl .album-art {
  border-radius: 50%;
  animation: spinVinyl 12s linear infinite;
  box-shadow: 0 0 0 4px #1a1a1a, 0 0 0 8px #2a2a2a, 0 8px 24px rgba(0, 0, 0, 0.6);
}
@keyframes spinVinyl {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* Toast Notification for New Requests */
.request-toast {
  position: absolute;
  top: 10px;
  left: 16px;
  right: 16px;
  z-index: 20;
  display: flex;
  align-items: center;
  gap: 10px;
  background: linear-gradient(135deg, rgba(29, 185, 84, 0.95), rgba(20, 140, 60, 0.95));
  color: #FFFFFF;
  padding: 8px 14px;
  border-radius: 10px;
  box-shadow: 0 4px 18px rgba(0, 0, 0, 0.4);
  animation: slideToast 0.35s cubic-bezier(0.16, 1, 0.3, 1);
  font-size: 0.85rem;
  font-weight: 600;
}
.toast-badge {
  background: rgba(0, 0, 0, 0.25);
  padding: 2px 8px;
  border-radius: 6px;
  font-size: 0.72rem;
  letter-spacing: 0.04em;
  font-family: var(--font-mono);
}
.toast-text {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
@keyframes slideToast {
  from { transform: translateY(-120%); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}

/* Idle State View */
.idle-view {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 6px 4px;
}
.idle-icon-wrapper {
  width: 56px;
  height: 56px;
  border-radius: 14px;
  background: rgba(29, 185, 84, 0.12);
  color: var(--accent-color);
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(29, 185, 84, 0.25);
  flex-shrink: 0;
  animation: pulseGlow 3s infinite ease-in-out;
}
@keyframes pulseGlow {
  0%, 100% { box-shadow: 0 0 10px rgba(29, 185, 84, 0.2); }
  50% { box-shadow: 0 0 22px rgba(29, 185, 84, 0.5); }
}
.idle-text-group {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.idle-title {
  font-size: 1.05rem;
  font-weight: 700;
  color: var(--text-primary);
}
.idle-desc {
  font-size: 0.85rem;
  color: var(--text-secondary);
}
.highlight-pill {
  background: rgba(255, 255, 255, 0.1);
  color: var(--accent-color);
  padding: 2px 8px;
  border-radius: 6px;
  font-family: var(--font-mono);
  font-weight: 600;
}

/* Active Playing View */
.playing-view {
  display: flex;
  align-items: center;
  gap: 16px;
  animation: fadeIn 0.4s ease-out;
}
@keyframes fadeIn {
  from { opacity: 0; transform: scale(0.97); }
  to { opacity: 1; transform: scale(1); }
}

/* Album Art */
.art-container {
  position: relative;
  width: 84px;
  height: 84px;
  flex-shrink: 0;
}
.album-art {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: var(--radius-art);
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.5);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

/* Sound Visualizer Bars */
.sound-visualizer {
  position: absolute;
  bottom: 6px;
  right: 6px;
  display: flex;
  align-items: flex-end;
  gap: 3px;
  height: 18px;
  background: rgba(0, 0, 0, 0.7);
  padding: 3px 5px;
  border-radius: 6px;
  backdrop-filter: blur(4px);
}
.sound-visualizer .bar {
  width: 3px;
  background: var(--accent-color);
  border-radius: 2px;
  animation: barBounce 0.8s ease-in-out infinite alternate;
}
.sound-visualizer .bar-1 { height: 6px; animation-delay: 0.1s; }
.sound-visualizer .bar-2 { height: 14px; animation-delay: 0.3s; }
.sound-visualizer .bar-3 { height: 9px; animation-delay: 0.15s; }
.sound-visualizer .bar-4 { height: 16px; animation-delay: 0.45s; }
.sound-visualizer .bar-5 { height: 7px; animation-delay: 0.25s; }

@keyframes barBounce {
  0% { height: 3px; opacity: 0.5; }
  100% { height: 16px; opacity: 1; }
}

/* Track Details Group */
.track-details {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

/* Meta Header Row (Status & Requester) */
.meta-header-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 2px;
}
.status-pill {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 0.68rem;
  font-family: var(--font-mono);
  font-weight: 700;
  letter-spacing: 0.06em;
  color: var(--accent-color);
  background: rgba(29, 185, 84, 0.12);
  padding: 2px 8px;
  border-radius: 20px;
  border: 1px solid rgba(29, 185, 84, 0.25);
}
.pulse-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--accent-color);
  animation: pulseDot 1.4s infinite ease-in-out;
}
@keyframes pulseDot {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.4); opacity: 0.5; }
}

.requester-badge {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  font-size: 0.75rem;
  color: #BF94FF;
  background: rgba(145, 70, 255, 0.14);
  border: 1px solid rgba(145, 70, 255, 0.3);
  padding: 2px 8px;
  border-radius: 12px;
  font-weight: 600;
}
.twitch-icon {
  flex-shrink: 0;
}

/* Track Title & Artist */
.track-title-wrapper {
  overflow: hidden;
  white-space: nowrap;
  position: relative;
}
.track-title {
  font-size: 1.15rem;
  font-weight: 700;
  color: var(--text-primary);
  display: inline-block;
}
.track-artist {
  font-size: 0.88rem;
  font-weight: 500;
  color: var(--text-secondary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Real-time Progress Bar */
.progress-container {
  margin-top: 6px;
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.progress-bar-bg {
  width: 100%;
  height: 5px;
  background: rgba(255, 255, 255, 0.12);
  border-radius: 6px;
  overflow: hidden;
}
.progress-bar-fill {
  height: 100%;
  background: var(--accent-color);
  border-radius: 6px;
  transition: width 0.3s linear;
  box-shadow: 0 0 10px var(--accent-glow);
}
.time-labels {
  display: flex;
  justify-content: space-between;
  font-size: 0.68rem;
  font-family: var(--font-mono);
  color: var(--text-secondary);
}

/* Utility Hidden */
.hidden {
  display: none !important;
}

/* ============================================================
   Theme 5: Waveform Compact
   Layout: [AlbumArt] [Title / Artist / Time ▌Waveform▌ Time]
   Bottom edge: thin progress bar
   ============================================================ */

/* Card override */
.theme-waveform {
  background: rgba(10, 12, 18, 0.72);
  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);
  border: none;
  border-radius: 10px;
  box-shadow: none;
  padding: 10px 14px 18px 10px;
  width: auto;
  min-height: unset;
  overflow: visible;
}

/* Hide status pill and requester on waveform theme */
.theme-waveform .meta-header-row {
  display: none !important;
}

/* Tighter art container */
.theme-waveform .art-container {
  width: 70px;
  height: 70px;
  flex-shrink: 0;
}

.theme-waveform .album-art {
  width: 70px;
  height: 70px;
  border-radius: 6px;
  border: none;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.7);
  filter: brightness(0.85);
}

/* Hide built-in visualizer overlay on art */
.theme-waveform .sound-visualizer {
  display: none !important;
}

/* Track details: title / artist / waveform-row stacked */
.theme-waveform .track-details {
  gap: 2px;
}

.theme-waveform .track-title {
  font-size: 1.05rem;
  font-weight: 700;
}

.theme-waveform .track-artist {
  font-size: 0.82rem;
  color: rgba(255, 255, 255, 0.65);
  margin-bottom: 4px;
}

/* Hide standard progress container on waveform theme */
.theme-waveform #progress-container {
  display: none !important;
}

/* === Waveform Row === */
.waveform-row {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 4px;
}

.wf-time {
  font-family: var(--font-mono);
  font-size: 0.72rem;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.75);
  white-space: nowrap;
  flex-shrink: 0;
}

/* Waveform bars container */
.waveform-bars {
  display: flex;
  align-items: center;
  gap: 1px;
  height: 22px;
  flex: 1;
  overflow: hidden;
}

/* Each individual bar — thin like real audio fingerprint */
.waveform-bars .wb {
  flex: 1;
  min-width: 1.5px;
  max-width: 4px;
  border-radius: 1px;
  background: var(--accent-color);
  animation: wbPulse var(--wdur, 0.9s) ease-in-out infinite alternate;
  opacity: 0.88;
  height: var(--wh, 10px);
}

/* 44 bars — heights mimic a real audio waveform fingerprint */
.waveform-bars .wb:nth-child(1)  { --wh: 6px;  --wdur: 0.82s; animation-delay: 0.00s; }
.waveform-bars .wb:nth-child(2)  { --wh: 13px; --wdur: 0.75s; animation-delay: 0.05s; }
.waveform-bars .wb:nth-child(3)  { --wh: 9px;  --wdur: 0.91s; animation-delay: 0.11s; }
.waveform-bars .wb:nth-child(4)  { --wh: 18px; --wdur: 0.68s; animation-delay: 0.03s; }
.waveform-bars .wb:nth-child(5)  { --wh: 7px;  --wdur: 0.85s; animation-delay: 0.17s; }
.waveform-bars .wb:nth-child(6)  { --wh: 20px; --wdur: 0.72s; animation-delay: 0.08s; }
.waveform-bars .wb:nth-child(7)  { --wh: 11px; --wdur: 0.95s; animation-delay: 0.22s; }
.waveform-bars .wb:nth-child(8)  { --wh: 16px; --wdur: 0.79s; animation-delay: 0.14s; }
.waveform-bars .wb:nth-child(9)  { --wh: 8px;  --wdur: 0.88s; animation-delay: 0.06s; }
.waveform-bars .wb:nth-child(10) { --wh: 19px; --wdur: 0.70s; animation-delay: 0.19s; }
.waveform-bars .wb:nth-child(11) { --wh: 5px;  --wdur: 0.93s; animation-delay: 0.27s; }
.waveform-bars .wb:nth-child(12) { --wh: 17px; --wdur: 0.76s; animation-delay: 0.10s; }
.waveform-bars .wb:nth-child(13) { --wh: 12px; --wdur: 0.84s; animation-delay: 0.32s; }
.waveform-bars .wb:nth-child(14) { --wh: 20px; --wdur: 0.67s; animation-delay: 0.04s; }
.waveform-bars .wb:nth-child(15) { --wh: 9px;  --wdur: 0.89s; animation-delay: 0.23s; }
.waveform-bars .wb:nth-child(16) { --wh: 15px; --wdur: 0.73s; animation-delay: 0.15s; }
.waveform-bars .wb:nth-child(17) { --wh: 6px;  --wdur: 0.97s; animation-delay: 0.37s; }
.waveform-bars .wb:nth-child(18) { --wh: 18px; --wdur: 0.78s; animation-delay: 0.09s; }
.waveform-bars .wb:nth-child(19) { --wh: 10px; --wdur: 0.86s; animation-delay: 0.28s; }
.waveform-bars .wb:nth-child(20) { --wh: 21px; --wdur: 0.65s; animation-delay: 0.01s; }
.waveform-bars .wb:nth-child(21) { --wh: 7px;  --wdur: 0.92s; animation-delay: 0.34s; }
.waveform-bars .wb:nth-child(22) { --wh: 16px; --wdur: 0.74s; animation-delay: 0.18s; }
.waveform-bars .wb:nth-child(23) { --wh: 13px; --wdur: 0.87s; animation-delay: 0.42s; }
.waveform-bars .wb:nth-child(24) { --wh: 19px; --wdur: 0.71s; animation-delay: 0.12s; }
.waveform-bars .wb:nth-child(25) { --wh: 8px;  --wdur: 0.94s; animation-delay: 0.25s; }
.waveform-bars .wb:nth-child(26) { --wh: 20px; --wdur: 0.69s; animation-delay: 0.07s; }
.waveform-bars .wb:nth-child(27) { --wh: 11px; --wdur: 0.83s; animation-delay: 0.38s; }
.waveform-bars .wb:nth-child(28) { --wh: 17px; --wdur: 0.77s; animation-delay: 0.20s; }
.waveform-bars .wb:nth-child(29) { --wh: 6px;  --wdur: 0.96s; animation-delay: 0.44s; }
.waveform-bars .wb:nth-child(30) { --wh: 14px; --wdur: 0.81s; animation-delay: 0.13s; }
.waveform-bars .wb:nth-child(31) { --wh: 9px;  --wdur: 0.90s; animation-delay: 0.30s; }
.waveform-bars .wb:nth-child(32) { --wh: 22px; --wdur: 0.66s; animation-delay: 0.02s; }
.waveform-bars .wb:nth-child(33) { --wh: 7px;  --wdur: 0.98s; animation-delay: 0.47s; }
.waveform-bars .wb:nth-child(34) { --wh: 15px; --wdur: 0.80s; animation-delay: 0.16s; }
.waveform-bars .wb:nth-child(35) { --wh: 12px; --wdur: 0.88s; animation-delay: 0.35s; }
.waveform-bars .wb:nth-child(36) { --wh: 18px; --wdur: 0.73s; animation-delay: 0.21s; }
.waveform-bars .wb:nth-child(37) { --wh: 5px;  --wdur: 1.00s; animation-delay: 0.49s; }
.waveform-bars .wb:nth-child(38) { --wh: 16px; --wdur: 0.76s; animation-delay: 0.26s; }
.waveform-bars .wb:nth-child(39) { --wh: 10px; --wdur: 0.85s; animation-delay: 0.40s; }
.waveform-bars .wb:nth-child(40) { --wh: 19px; --wdur: 0.70s; animation-delay: 0.08s; }
.waveform-bars .wb:nth-child(41) { --wh: 8px;  --wdur: 0.93s; animation-delay: 0.46s; }
.waveform-bars .wb:nth-child(42) { --wh: 14px; --wdur: 0.78s; animation-delay: 0.31s; }
.waveform-bars .wb:nth-child(43) { --wh: 11px; --wdur: 0.87s; animation-delay: 0.43s; }
.waveform-bars .wb:nth-child(44) { --wh: 17px; --wdur: 0.72s; animation-delay: 0.24s; }

@keyframes wbPulse {
  0%   { height: 3px; opacity: 0.35; }
  100% { height: var(--wh, 12px); opacity: 1; }
}

/* === Bottom Edge Progress Bar (waveform theme) === */
.bottom-progress-bar {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: rgba(255, 255, 255, 0.10);
  border-radius: 0 0 10px 10px;
  overflow: hidden;
}

.bottom-progress-fill {
  height: 100%;
  background: var(--accent-color);
  box-shadow: 0 0 8px var(--accent-glow);
  transition: width 0.3s linear;
  border-radius: 0 0 10px 10px;
}

/* ============================================================
   Theme 6: Lo-Fi Cozy Cafe
   Aesthetic: Warm dark cocoa glass, polaroid album art,
   washi tape accent, caramel/honey glow, cozy typography
   ============================================================ */

.theme-lofi-cafe {
  background: linear-gradient(135deg, rgba(38, 28, 24, 0.88), rgba(22, 16, 14, 0.94));
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border: 1.5px solid rgba(212, 163, 115, 0.32);
  border-radius: 20px;
  box-shadow: 0 14px 40px rgba(12, 8, 6, 0.65), 0 0 24px rgba(212, 163, 115, 0.15);
  position: relative;
}

/* Subtle warm highlight on top edge */
.theme-lofi-cafe::before {
  content: '';
  position: absolute;
  top: 0;
  left: 15%;
  right: 15%;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(254, 250, 224, 0.35), transparent);
  pointer-events: none;
}

/* Polaroid style album art */
.theme-lofi-cafe .art-container {
  position: relative;
  width: 88px;
  height: 88px;
  padding: 4px;
  background: #fdfbf7;
  border-radius: 12px;
  box-shadow: 0 8px 22px rgba(0, 0, 0, 0.5);
  transform: rotate(-1.5deg);
  transition: transform 0.3s ease;
}

.theme-lofi-cafe .art-container:hover {
  transform: rotate(0deg) scale(1.02);
}

.theme-lofi-cafe .album-art {
  width: 100%;
  height: 100%;
  border-radius: 8px;
  border: none;
  box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.15);
}

/* Washi tape sticker over the top corner of the album art */
.theme-lofi-cafe .art-container::after {
  content: '';
  position: absolute;
  top: -7px;
  left: 18px;
  width: 38px;
  height: 14px;
  background: rgba(226, 149, 120, 0.7);
  backdrop-filter: blur(2px);
  transform: rotate(-6deg);
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.18);
  border-left: 2px dashed rgba(255, 255, 255, 0.3);
  border-right: 2px dashed rgba(255, 255, 255, 0.3);
  pointer-events: none;
  z-index: 5;
}

/* Status Pill in cozy latte style */
.theme-lofi-cafe .status-pill {
  background: rgba(212, 163, 115, 0.2);
  border: 1px solid rgba(212, 163, 115, 0.45);
  color: #faedcd;
  border-radius: 16px;
  font-weight: 600;
  letter-spacing: 0.05em;
  padding: 3px 10px;
}

.theme-lofi-cafe .pulse-dot {
  background: #e29578;
  box-shadow: 0 0 8px rgba(226, 149, 120, 0.85);
}

/* Requester badge in soft cream/peach tone */
.theme-lofi-cafe .requester-badge {
  background: rgba(226, 149, 120, 0.18);
  border: 1px solid rgba(226, 149, 120, 0.38);
  color: #fbc3bc;
  border-radius: 16px;
}

/* Track title & artist */
.theme-lofi-cafe .track-title {
  color: #fefae0;
  font-weight: 700;
  letter-spacing: 0.01em;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
}

.theme-lofi-cafe .track-artist {
  color: #d4a373;
  font-weight: 500;
}

/* Sound Visualizer with warm pastel bars */
.theme-lofi-cafe .sound-visualizer {
  background: rgba(30, 22, 18, 0.82);
  border: 1px solid rgba(212, 163, 115, 0.25);
  border-radius: 8px;
}

.theme-lofi-cafe .sound-visualizer .bar {
  background: linear-gradient(180deg, #e29578, #d4a373);
  border-radius: 3px;
}

/* Progress bar in caramel-honey gradient */
.theme-lofi-cafe .progress-bar-bg {
  height: 6px;
  background: rgba(254, 250, 224, 0.12);
  border-radius: 10px;
}

.theme-lofi-cafe .progress-bar-fill {
  height: 100%;
  background: linear-gradient(90deg, #d4a373, #e29578);
  border-radius: 10px;
  box-shadow: 0 0 12px rgba(226, 149, 120, 0.5);
}

.theme-lofi-cafe .time-labels {
  color: #d4a373;
  font-weight: 500;
}

/* Idle state styling */
.theme-lofi-cafe .idle-icon-wrapper {
  background: rgba(212, 163, 115, 0.16);
  border-color: rgba(212, 163, 115, 0.35);
  color: #d4a373;
}

.theme-lofi-cafe .idle-title {
  color: #fefae0;
}

.theme-lofi-cafe .idle-desc {
  color: #d4a373;
}

.theme-lofi-cafe .highlight-pill {
  background: rgba(212, 163, 115, 0.2);
  color: #faedcd;
}
