@import url('https://fonts.googleapis.com/css2?family=Bai+Jamjuree:wght@600;700&family=Chakra+Petch:wght@600;700;800&family=Itim&family=Kanit:wght@600;700;800;900&family=Mali:wght@600;700&family=Mitr:wght@600;700&family=Pridi:wght@600;700;800&family=Prompt:wght@600;700;800;900&family=Sarabun:wght@600;700;800&display=swap');

:root {
  --counter-accent: #f59e0b;
  --counter-bg: rgba(15, 23, 42, 0.9);
  --counter-text: #ffffff;
  --counter-border: rgba(245, 158, 11, 0.5);
  --counter-glow: rgba(245, 158, 11, 0.25);
  --counter-scale: 1;
  --counter-font: 'Prompt', 'Kanit', -apple-system, sans-serif;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  background: transparent;
  overflow: hidden;
  font-family: var(--counter-font);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  display: flex;
  align-items: center;
  justify-content: flex-start;
  min-height: 100vh;
  padding: 24px;
}

.counter-widget-root {
  transform: scale(var(--counter-scale));
  transform-origin: left center;
  transition: transform 0.25s ease;
  user-select: none;
}

/* ============================================================
   Base Counter Card
   ============================================================ */
.counter-card {
  position: relative;
  display: inline-flex;
  align-items: center;
  gap: 1.25rem;
  padding: 0.85rem 1.65rem 0.85rem 1.15rem;
  border-radius: 20px;
  background: var(--counter-bg);
  border: 1.5px solid var(--counter-border);
  box-shadow: 0 10px 32px -4px rgba(0, 0, 0, 0.5), 0 0 24px -4px var(--counter-glow);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  overflow: hidden;
}

.counter-glow-bg {
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle at center, var(--counter-glow) 0%, transparent 60%);
  pointer-events: none;
  z-index: 0;
  opacity: 0.8;
  transition: opacity 0.3s ease;
}

/* Header: Icon + Title */
.counter-header {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  gap: 0.75rem;
}

.counter-icon-box {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid var(--counter-border);
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.25);
  color: var(--counter-accent);
  transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  flex-shrink: 0;
}

.counter-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: var(--counter-accent);
}

.counter-icon svg {
  width: 24px;
  height: 24px;
  display: block;
  stroke: currentColor;
  filter: drop-shadow(0 2px 6px rgba(0, 0, 0, 0.35));
}

.counter-icon-box.pop {
  transform: scale(1.25) rotate(-6deg);
}

.counter-title-wrap {
  display: flex;
  flex-direction: column;
}

.counter-title {
  font-family: var(--counter-font);
  font-size: 1rem;
  font-weight: 700;
  color: var(--counter-text);
  letter-spacing: -0.01em;
  white-space: nowrap;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
}

/* Value Display & Unit */
.counter-value-container {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: baseline;
  gap: 0.45rem;
  padding-left: 0.5rem;
  border-left: 1px solid var(--counter-border);
}

.counter-value-wrapper {
  position: relative;
  display: inline-block;
}

.counter-value {
  font-family: var(--counter-font);
  font-variant-numeric: tabular-nums;
  font-size: 2.35rem;
  font-weight: 800;
  line-height: 1;
  color: var(--counter-accent);
  text-shadow: 0 0 16px var(--counter-glow), 0 2px 4px rgba(0, 0, 0, 0.5);
  display: inline-block;
  transition: transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1), color 0.2s ease;
}

.counter-value.bump {
  transform: scale(1.28);
  color: #ffffff;
  text-shadow: 0 0 24px rgba(255, 255, 255, 0.9), 0 0 36px var(--counter-accent);
}

.counter-delta {
  position: absolute;
  top: -14px;
  right: -24px;
  font-size: 0.95rem;
  font-weight: 900;
  color: #34d399;
  text-shadow: 0 0 8px rgba(52, 211, 153, 0.8);
  opacity: 0;
  pointer-events: none;
  transform: translateY(0);
}

.counter-delta.float-up {
  animation: deltaFloat 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

.counter-delta.negative {
  color: #f87171;
  text-shadow: 0 0 8px rgba(248, 113, 113, 0.8);
}

@keyframes deltaFloat {
  0% {
    opacity: 1;
    transform: translateY(0) scale(1.1);
  }
  60% {
    opacity: 1;
    transform: translateY(-20px) scale(1);
  }
  100% {
    opacity: 0;
    transform: translateY(-32px) scale(0.9);
  }
}

.counter-unit {
  font-family: var(--counter-font);
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--counter-text);
  opacity: 0.75;
  letter-spacing: 0.02em;
  white-space: nowrap;
}

.counter-accent-line {
  position: absolute;
  bottom: 0;
  left: 10%;
  right: 10%;
  height: 2px;
  background: linear-gradient(90deg, transparent, var(--counter-accent), transparent);
  opacity: 0.8;
}

/* ============================================================
   THEME 0: Clean HUD (Frameless / Text + Icon Only)
   ============================================================ */
.theme-clean-hud {
  background: transparent !important;
  border: none !important;
  border-radius: 0 !important;
  box-shadow: none !important;
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
  padding: 0.2rem 0.4rem !important;
  gap: 0.85rem !important;
  position: relative;
  overflow: visible !important;
}

.theme-clean-hud::before,
.theme-clean-hud::after {
  content: none !important;
  display: none !important;
}

.theme-clean-hud .counter-glow-bg {
  display: none !important;
}

.theme-clean-hud .counter-accent-line {
  display: none !important;
}

.theme-clean-hud .counter-header {
  gap: 0.55rem;
}

.theme-clean-hud .counter-icon-box {
  width: auto !important;
  height: auto !important;
  background: transparent !important;
  border: none !important;
  border-radius: 0 !important;
  box-shadow: none !important;
  color: var(--counter-accent);
}

.theme-clean-hud .counter-icon svg {
  width: 26px;
  height: 26px;
  filter: drop-shadow(0 2px 5px rgba(0, 0, 0, 0.95)) drop-shadow(0 0 8px rgba(0, 0, 0, 0.6));
}

.theme-clean-hud .counter-title {
  font-family: var(--counter-font);
  font-size: 1.15rem;
  font-weight: 800;
  color: var(--counter-text);
  text-shadow: 0 2px 6px rgba(0, 0, 0, 0.95), 0 0 12px rgba(0, 0, 0, 0.8), 1px 1px 2px #000;
}

.theme-clean-hud .counter-value-container {
  border-left: none !important;
  padding-left: 0.25rem !important;
  gap: 0.35rem;
}

.theme-clean-hud .counter-value {
  font-family: var(--counter-font);
  font-variant-numeric: tabular-nums;
  font-size: 2.35rem;
  font-weight: 900;
  color: var(--counter-accent);
  text-shadow: 0 2px 6px rgba(0, 0, 0, 0.95), 0 0 16px var(--counter-glow), 1px 1px 2px #000;
}

.theme-clean-hud .counter-unit {
  font-family: var(--counter-font);
  font-size: 0.95rem;
  font-weight: 700;
  color: var(--counter-text);
  opacity: 0.95;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.95), 0 0 8px rgba(0, 0, 0, 0.8);
}

/* ============================================================
   THEME 0B: Minimal Compact (Doppelrand / Obsidian Capsule)
   ============================================================ */
.theme-minimal-clean {
  border-radius: 9999px;
  background: var(--counter-bg);
  border: 1.5px solid var(--counter-border);
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.55), inset 0 1px 0 rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  padding: 0.55rem 1.35rem 0.55rem 0.85rem;
  gap: 0.95rem;
}

.theme-minimal-clean .counter-glow-bg {
  display: none !important;
}

.theme-minimal-clean .counter-accent-line {
  display: none !important;
}

.theme-minimal-clean .counter-icon-box {
  width: 34px;
  height: 34px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.09);
  border: 1px solid var(--counter-border);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.2);
}

.theme-minimal-clean .counter-icon svg {
  width: 18px;
  height: 18px;
}

.theme-minimal-clean .counter-title {
  font-family: var(--counter-font);
  font-size: 0.95rem;
  font-weight: 700;
  color: var(--counter-text);
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.6);
}

.theme-minimal-clean .counter-value-container {
  border-left: 1px solid var(--counter-border);
  padding-left: 0.85rem;
  gap: 0.35rem;
}

.theme-minimal-clean .counter-value {
  font-family: var(--counter-font);
  font-variant-numeric: tabular-nums;
  font-size: 1.95rem;
  font-weight: 800;
  color: var(--counter-accent);
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.6);
}

.theme-minimal-clean .counter-unit {
  font-family: var(--counter-font);
  font-size: 0.82rem;
  font-weight: 600;
  color: var(--counter-text);
  opacity: 0.85;
}

/* ============================================================
   THEME 1: Minimal Glass (VisionOS / Liquid Aurora Glass)
   ============================================================ */
.theme-minimal-glass {
  border-radius: 9999px;
  background: var(--counter-bg);
  border: 1.5px solid var(--counter-border);
  box-shadow: 0 12px 36px rgba(0, 0, 0, 0.45), 0 0 24px var(--counter-glow), inset 0 1.5px 0 rgba(255, 255, 255, 0.35);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
}

.theme-minimal-glass .counter-icon-box {
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.08);
  border-color: var(--counter-border);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.25);
}

/* ============================================================
   THEME 2: Neon Cyberpunk (Tactical HUD / Night City Cyberware)
   ============================================================ */
.theme-cyberpunk {
  border-radius: 4px;
  clip-path: polygon(0 0, calc(100% - 16px) 0, 100% 16px, 100% 100%, 16px 100%, 0 calc(100% - 16px));
  background: linear-gradient(135deg, rgba(8, 12, 22, 0.95) 0%, rgba(15, 23, 42, 0.92) 100%);
  border: 2px solid var(--counter-border);
  box-shadow: 0 0 28px var(--counter-glow), inset 0 0 18px rgba(0, 0, 0, 0.85);
  padding: 0.95rem 1.85rem 0.95rem 1.3rem;
  position: relative;
}

/* Diagonal Hazard Caution Stripes Header */
.theme-cyberpunk::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 3px;
  background: repeating-linear-gradient(-45deg, var(--counter-accent), var(--counter-accent) 6px, transparent 6px, transparent 12px);
  opacity: 0.85;
}

/* Tactical Watermark */
.theme-cyberpunk::after {
  content: 'SYS.LOC // 0x4F';
  position: absolute;
  bottom: 2px;
  right: 18px;
  font-size: 0.52rem;
  font-family: monospace;
  letter-spacing: 0.14em;
  color: var(--counter-accent);
  opacity: 0.7;
}

.theme-cyberpunk .counter-title {
  font-family: var(--counter-font);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  font-weight: 800;
  color: var(--counter-text);
  text-shadow: 0 0 12px var(--counter-glow);
}

.theme-cyberpunk .counter-icon-box {
  border-radius: 4px;
  border: 1.5px solid var(--counter-border);
  background: rgba(0, 0, 0, 0.5);
  box-shadow: 0 0 12px var(--counter-glow);
  color: var(--counter-accent);
}

.theme-cyberpunk .counter-value {
  font-family: var(--counter-font);
  font-weight: 900;
  color: var(--counter-accent);
  text-shadow: 0 0 16px var(--counter-accent), 0 0 32px var(--counter-glow);
}

.theme-cyberpunk .counter-unit {
  font-family: var(--counter-font);
  color: var(--counter-text);
  font-weight: 700;
  opacity: 0.85;
}

.theme-cyberpunk.bump-card {
  animation: cyberGlitch 0.35s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

@keyframes cyberGlitch {
  0% { transform: scale(var(--counter-scale)) translate(0); }
  20% { transform: scale(var(--counter-scale)) translate(-3px, 2px); filter: hue-rotate(90deg); }
  40% { transform: scale(var(--counter-scale)) translate(3px, -2px); filter: hue-rotate(-90deg); }
  60% { transform: scale(var(--counter-scale)) translate(-2px, -1px); }
  80% { transform: scale(var(--counter-scale)) translate(2px, 1px); }
  100% { transform: scale(var(--counter-scale)) translate(0); }
}

/* ============================================================
   THEME 3: Souls / Dark Fantasy (Elden Ring / Sacred Relic)
   ============================================================ */
.theme-souls {
  border-radius: 8px;
  background: radial-gradient(circle at center, rgba(32, 22, 16, 0.95) 0%, rgba(10, 8, 7, 0.98) 100%), var(--counter-bg);
  border: 2px solid var(--counter-border);
  box-shadow: 0 14px 40px rgba(0, 0, 0, 0.9), 0 0 28px var(--counter-glow), inset 0 0 24px rgba(0, 0, 0, 0.85);
  padding: 0.95rem 1.95rem 0.95rem 1.4rem;
  position: relative;
}

/* Filigree Corner Accents */
.theme-souls::before {
  content: '✦';
  position: absolute;
  top: 4px;
  left: 7px;
  font-size: 0.72rem;
  color: var(--counter-border);
  opacity: 0.85;
  filter: drop-shadow(0 0 4px var(--counter-glow));
}

.theme-souls::after {
  content: '✦';
  position: absolute;
  bottom: 4px;
  right: 7px;
  font-size: 0.72rem;
  color: var(--counter-border);
  opacity: 0.85;
  filter: drop-shadow(0 0 4px var(--counter-glow));
}

.theme-souls .counter-title {
  font-family: var(--counter-font);
  font-weight: 800;
  letter-spacing: 0.08em;
  color: var(--counter-text);
  text-transform: uppercase;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.9);
}

.theme-souls .counter-icon-box {
  background: rgba(255, 255, 255, 0.05);
  border: 1.5px solid var(--counter-border);
  border-radius: 8px;
  box-shadow: 0 0 14px var(--counter-glow);
  color: var(--counter-accent);
}

.theme-souls .counter-value {
  font-family: var(--counter-font);
  font-weight: 900;
  color: var(--counter-accent);
  text-shadow: 0 0 20px var(--counter-glow), 0 0 35px var(--counter-glow), 0 2px 4px rgba(0, 0, 0, 0.9);
}

.theme-souls .counter-unit {
  font-family: var(--counter-font);
  color: var(--counter-text);
  font-weight: 700;
  opacity: 0.85;
}

.theme-souls.bump-card {
  animation: soulsBonfirePulse 0.4s ease-out;
}

@keyframes soulsBonfirePulse {
  0% { transform: scale(var(--counter-scale)); filter: brightness(1); }
  50% { transform: scale(calc(var(--counter-scale) * 1.04)); filter: brightness(1.4) drop-shadow(0 0 24px var(--counter-glow)); }
  100% { transform: scale(var(--counter-scale)); filter: brightness(1); }
}
/* ============================================================
   THEME 4: Retro Arcade (90s Neo-Geo / Pixel CRT Cabinet)
   ============================================================ */
.theme-arcade {
  border-radius: 0;
  background: #0d0c1d;
  border: 4px solid var(--counter-border);
  box-shadow: 5px 5px 0px #000, 9px 9px 0px var(--counter-border);
  position: relative;
  overflow: hidden;
  padding: 0.95rem 1.85rem 0.95rem 1.3rem;
}

/* CRT Scanlines Overlay */
.theme-arcade::before {
  content: '';
  position: absolute;
  inset: 0;
  background: repeating-linear-gradient(0deg, rgba(0, 0, 0, 0.4) 0px, rgba(0, 0, 0, 0.4) 2px, transparent 2px, transparent 4px);
  pointer-events: none;
  z-index: 2;
  opacity: 0.65;
}

/* 1UP Retro Flashing Badge */
.theme-arcade::after {
  content: '▶ 1UP';
  position: absolute;
  top: 4px;
  right: 10px;
  font-size: 0.6rem;
  font-weight: 900;
  letter-spacing: 0.12em;
  color: var(--counter-accent);
  animation: arcadeCoinBlink 0.9s steps(2, start) infinite;
  z-index: 3;
}

@keyframes arcadeCoinBlink {
  to { visibility: hidden; }
}

.theme-arcade .counter-title {
  font-family: var(--counter-font);
  font-size: 1rem;
  font-weight: 800;
  line-height: 1.3;
  color: var(--counter-text);
  text-shadow: 2px 2px 0px rgba(0, 0, 0, 0.9);
  position: relative;
  z-index: 3;
}

.theme-arcade .counter-icon-box {
  border-radius: 0;
  border: 2px solid var(--counter-accent);
  background: rgba(0, 0, 0, 0.6);
  box-shadow: 3px 3px 0px #000;
  color: var(--counter-accent);
  position: relative;
  z-index: 3;
}

.theme-arcade .counter-value {
  font-family: var(--counter-font);
  font-size: 2.2rem;
  font-weight: 900;
  color: var(--counter-accent);
  text-shadow: 3px 3px 0px #000, 0 0 14px var(--counter-accent);
  position: relative;
  z-index: 3;
}

.theme-arcade .counter-unit {
  font-family: var(--counter-font);
  font-size: 0.85rem;
  font-weight: 800;
  color: var(--counter-text);
  opacity: 0.9;
  text-shadow: 2px 2px 0px #000;
  position: relative;
  z-index: 3;
}

.theme-arcade .counter-value-container {
  border-left: 3px solid var(--counter-accent);
}

.theme-arcade.bump-card {
  animation: arcadeScreenShake 0.3s cubic-bezier(0.36, 0.07, 0.19, 0.97);
}

@keyframes arcadeScreenShake {
  0% { transform: scale(var(--counter-scale)) translate(0); }
  25% { transform: scale(var(--counter-scale)) translate(3px, -3px); }
  50% { transform: scale(var(--counter-scale)) translate(-3px, 3px); }
  75% { transform: scale(var(--counter-scale)) translate(2px, -1px); }
  100% { transform: scale(var(--counter-scale)) translate(0); }
}

/* ============================================================
   THEME 5: Mecha Cockpit (Evangelion / Armored Core HUD)
   ============================================================ */
.theme-mecha-hud {
  border-radius: 2px;
  background: linear-gradient(180deg, rgba(16, 22, 32, 0.96) 0%, rgba(8, 11, 18, 0.98) 100%);
  border: 1.5px solid var(--counter-border);
  border-left: 6px solid var(--counter-accent);
  box-shadow: 0 12px 36px rgba(0, 0, 0, 0.8), inset 0 0 16px rgba(0, 0, 0, 0.9);
  padding: 0.95rem 1.85rem 0.95rem 1.35rem;
  position: relative;
}

.theme-mecha-hud::before {
  content: 'TACTICAL // DEPLOY';
  position: absolute;
  top: 3px;
  left: 12px;
  font-size: 0.52rem;
  font-weight: 900;
  font-family: monospace;
  letter-spacing: 0.18em;
  color: var(--counter-accent);
  opacity: 0.85;
}

.theme-mecha-hud::after {
  content: '';
  position: absolute;
  bottom: 0;
  right: 0;
  width: 14px;
  height: 14px;
  background: linear-gradient(135deg, transparent 50%, var(--counter-accent) 50%);
}

.theme-mecha-hud .counter-title {
  font-family: var(--counter-font);
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--counter-text);
  text-shadow: 0 2px 6px rgba(0, 0, 0, 0.9);
}

.theme-mecha-hud .counter-icon-box {
  background: rgba(0, 0, 0, 0.6);
  border: 1.5px solid var(--counter-border);
  border-radius: 3px;
  box-shadow: 0 0 12px var(--counter-glow);
  color: var(--counter-accent);
}

.theme-mecha-hud .counter-value {
  font-family: var(--counter-font);
  font-weight: 900;
  color: var(--counter-accent);
  text-shadow: 0 0 18px var(--counter-glow), 0 2px 6px rgba(0, 0, 0, 0.9);
}

.theme-mecha-hud .counter-unit {
  font-family: var(--counter-font);
  color: var(--counter-text);
  font-weight: 700;
  opacity: 0.85;
}

.theme-mecha-hud.bump-card {
  animation: mechaRecoil 0.35s ease-out;
}

@keyframes mechaRecoil {
  0% { transform: scale(var(--counter-scale)) translateX(0); }
  30% { transform: scale(var(--counter-scale)) translateX(6px); }
  100% { transform: scale(var(--counter-scale)) translateX(0); }
}
