@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Chakra+Petch:wght@600;700;800&family=Cinzel:wght@700;900&family=Kanit:wght@600;700;800;900&family=Press+Start+2P&family=Prompt:wght@600;700;800;900&family=Rajdhani:wght@600;700;800&display=swap');

:root {
  --counter-accent: #f59e0b;
  --counter-bg: rgba(15, 23, 42, 0.9);
  --counter-text: #ffffff;
  --counter-border: rgba(245, 158, 11, 0.5);
  --counter-glow: rgba(245, 158, 11, 0.25);
  --counter-scale: 1;
  --counter-font: 'Prompt', 'Kanit', -apple-system, sans-serif;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  background: transparent;
  overflow: hidden;
  font-family: var(--counter-font);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  display: flex;
  align-items: center;
  justify-content: flex-start;
  min-height: 100vh;
  padding: 24px;
}

.counter-widget-root {
  transform: scale(var(--counter-scale));
  transform-origin: left center;
  transition: transform 0.25s ease;
  user-select: none;
}

/* ============================================================
   Base Counter Card
   ============================================================ */
.counter-card {
  position: relative;
  display: inline-flex;
  align-items: center;
  gap: 1.25rem;
  padding: 0.85rem 1.65rem 0.85rem 1.15rem;
  border-radius: 20px;
  background: var(--counter-bg);
  border: 1.5px solid var(--counter-border);
  box-shadow: 0 10px 32px -4px rgba(0, 0, 0, 0.5), 0 0 24px -4px var(--counter-glow);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  overflow: hidden;
}

.counter-glow-bg {
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle at center, var(--counter-glow) 0%, transparent 60%);
  pointer-events: none;
  z-index: 0;
  opacity: 0.8;
  transition: opacity 0.3s ease;
}

/* Header: Icon + Title */
.counter-header {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  gap: 0.75rem;
}

.counter-icon-box {
  width: 44px;
  height: 44px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid var(--counter-border);
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.25);
  color: var(--counter-accent);
  transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  flex-shrink: 0;
}

.counter-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: var(--counter-accent);
}

.counter-icon svg {
  width: 24px;
  height: 24px;
  display: block;
  stroke: currentColor;
  filter: drop-shadow(0 2px 6px rgba(0, 0, 0, 0.35));
}

.counter-icon-box.pop {
  transform: scale(1.25) rotate(-6deg);
}

.counter-title-wrap {
  display: flex;
  flex-direction: column;
}

.counter-title {
  font-family: var(--counter-font);
  font-size: 1rem;
  font-weight: 700;
  color: var(--counter-text);
  letter-spacing: -0.01em;
  white-space: nowrap;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
}

/* Value Display & Unit */
.counter-value-container {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: baseline;
  gap: 0.45rem;
  padding-left: 0.5rem;
  border-left: 1px solid var(--counter-border);
}

.counter-value-wrapper {
  position: relative;
  display: inline-block;
}

.counter-value {
  font-family: var(--counter-font);
  font-variant-numeric: tabular-nums;
  font-size: 2.35rem;
  font-weight: 800;
  line-height: 1;
  color: var(--counter-accent);
  text-shadow: 0 0 16px var(--counter-glow), 0 2px 4px rgba(0, 0, 0, 0.5);
  display: inline-block;
  transition: transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1), color 0.2s ease;
}

.counter-value.bump {
  transform: scale(1.28);
  color: #ffffff;
  text-shadow: 0 0 24px rgba(255, 255, 255, 0.9), 0 0 36px var(--counter-accent);
}

.counter-delta {
  position: absolute;
  top: -14px;
  right: -24px;
  font-size: 0.95rem;
  font-weight: 900;
  color: #34d399;
  text-shadow: 0 0 8px rgba(52, 211, 153, 0.8);
  opacity: 0;
  pointer-events: none;
  transform: translateY(0);
}

.counter-delta.float-up {
  animation: deltaFloat 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

.counter-delta.negative {
  color: #f87171;
  text-shadow: 0 0 8px rgba(248, 113, 113, 0.8);
}

@keyframes deltaFloat {
  0% {
    opacity: 1;
    transform: translateY(0) scale(1.1);
  }
  60% {
    opacity: 1;
    transform: translateY(-20px) scale(1);
  }
  100% {
    opacity: 0;
    transform: translateY(-32px) scale(0.9);
  }
}

.counter-unit {
  font-family: var(--counter-font);
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--counter-text);
  opacity: 0.75;
  letter-spacing: 0.02em;
  white-space: nowrap;
}

.counter-accent-line {
  position: absolute;
  bottom: 0;
  left: 10%;
  right: 10%;
  height: 2px;
  background: linear-gradient(90deg, transparent, var(--counter-accent), transparent);
  opacity: 0.8;
}

/* ============================================================
   THEME 0: Clean HUD (Border-free / Transparent / Uncluttered)
   ============================================================ */
.theme-clean-hud {
  background: transparent !important;
  border: none !important;
  box-shadow: none !important;
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
  padding: 4px 8px;
  gap: 0.85rem;
}

.theme-clean-hud .counter-glow-bg {
  display: none !important;
}

.theme-clean-hud .counter-accent-line {
  display: none !important;
}

.theme-clean-hud .counter-header {
  gap: 0.5rem;
}

.theme-clean-hud .counter-icon-box {
  width: auto;
  height: auto;
  background: transparent !important;
  border: none !important;
  box-shadow: none !important;
  border-radius: 0;
  padding: 0;
}

.theme-clean-hud .counter-icon svg {
  width: 22px;
  height: 22px;
  filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.9)) drop-shadow(0 0 2px rgba(0, 0, 0, 0.9));
}

.theme-clean-hud .counter-title {
  font-family: var(--counter-font);
  font-size: 1.05rem;
  font-weight: 700;
  letter-spacing: -0.01em;
  color: var(--counter-text);
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.95), 0 0 4px rgba(0, 0, 0, 0.9), 1px 1px 2px rgba(0, 0, 0, 1), -1px -1px 2px rgba(0, 0, 0, 1);
}

.theme-clean-hud .counter-value-container {
  border-left: none;
  padding-left: 0;
  gap: 0.35rem;
}

.theme-clean-hud .counter-value {
  font-family: var(--counter-font);
  font-variant-numeric: tabular-nums;
  font-size: 2.3rem;
  font-weight: 800;
  color: var(--counter-accent);
  text-shadow: 0 2px 6px rgba(0, 0, 0, 0.95), 0 0 8px rgba(0, 0, 0, 0.85), 1px 1px 2px #000, -1px -1px 2px #000;
}

.theme-clean-hud .counter-value.bump {
  transform: scale(1.2);
  color: var(--counter-accent);
  text-shadow: 0 2px 8px rgba(0, 0, 0, 1), 0 0 16px var(--counter-accent);
}

.theme-clean-hud .counter-delta {
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.95), 0 0 3px #000;
}

.theme-clean-hud .counter-unit {
  font-family: var(--counter-font);
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--counter-text);
  opacity: 0.95;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.95), 0 0 3px rgba(0, 0, 0, 0.9), 1px 1px 2px rgba(0, 0, 0, 1);
}

/* ============================================================
   THEME 0B: Minimal Compact (Sleek pill / Non-intrusive)
   ============================================================ */
.theme-minimal-clean {
  border-radius: 9999px;
  background: rgba(15, 23, 42, 0.55);
  border: 1px solid rgba(255, 255, 255, 0.15);
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.35);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  padding: 0.5rem 1.15rem 0.5rem 0.85rem;
  gap: 0.9rem;
}

.theme-minimal-clean .counter-glow-bg {
  display: none !important;
}

.theme-minimal-clean .counter-accent-line {
  display: none !important;
}

.theme-minimal-clean .counter-icon-box {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.08);
  border: none;
  box-shadow: none;
}

.theme-minimal-clean .counter-icon svg {
  width: 18px;
  height: 18px;
}

.theme-minimal-clean .counter-title {
  font-family: var(--counter-font);
  font-size: 0.92rem;
  font-weight: 600;
  color: var(--counter-text);
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.6);
}

.theme-minimal-clean .counter-value-container {
  border-left: 1px solid rgba(255, 255, 255, 0.15);
  padding-left: 0.75rem;
  gap: 0.35rem;
}

.theme-minimal-clean .counter-value {
  font-family: var(--counter-font);
  font-variant-numeric: tabular-nums;
  font-size: 1.85rem;
  font-weight: 800;
  color: var(--counter-accent);
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.5);
}

.theme-minimal-clean .counter-unit {
  font-family: var(--counter-font);
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--counter-text);
  opacity: 0.8;
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.5);
}

/* ============================================================
   THEME 1: Minimal Glass
   ============================================================ */
.theme-minimal-glass {
  border-radius: 9999px;
  background: var(--counter-bg);
  border: 1.5px solid var(--counter-border);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4), 0 0 20px var(--counter-glow), inset 0 1px 0 rgba(255, 255, 255, 0.15);
}

.theme-minimal-glass .counter-icon-box {
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.07);
  border-color: var(--counter-border);
}

/* ============================================================
   THEME 2: Neon Cyberpunk
   ============================================================ */
.theme-cyberpunk {
  border-radius: 4px;
  clip-path: polygon(0 0, calc(100% - 14px) 0, 100% 14px, 100% 100%, 14px 100%, 0 calc(100% - 14px));
  background: var(--counter-bg);
  border: 2px solid var(--counter-border);
  box-shadow: 0 0 24px var(--counter-glow), inset 0 0 14px var(--counter-glow);
}

.theme-cyberpunk .counter-title {
  font-family: 'Outfit', sans-serif;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--counter-text);
  text-shadow: 0 0 12px var(--counter-glow);
}

.theme-cyberpunk .counter-icon-box {
  border-radius: 4px;
  border-color: var(--counter-border);
  background: var(--counter-glow);
  color: var(--counter-accent);
}

.theme-cyberpunk .counter-value {
  color: var(--counter-accent);
  text-shadow: 0 0 14px var(--counter-accent), 0 0 28px var(--counter-glow);
}

.theme-cyberpunk .counter-unit {
  color: var(--counter-text);
  opacity: 0.85;
}

/* ============================================================
   THEME 3: Souls / Dark Fantasy
   ============================================================ */
.theme-souls {
  border-radius: 12px;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.04) 0%, rgba(0, 0, 0, 0.4) 100%), var(--counter-bg);
  border: 2px solid var(--counter-border);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.8), 0 0 24px var(--counter-glow);
}

.theme-souls .counter-title {
  font-family: 'Cinzel', serif;
  font-weight: 900;
  letter-spacing: 0.08em;
  color: var(--counter-text);
  text-transform: uppercase;
}

.theme-souls .counter-icon-box {
  background: rgba(255, 255, 255, 0.05);
  border-color: var(--counter-border);
  border-radius: 8px;
  color: var(--counter-accent);
}

.theme-souls .counter-value {
  font-family: 'Cinzel', serif;
  color: var(--counter-accent);
  text-shadow: 0 0 18px var(--counter-glow), 0 0 30px var(--counter-glow);
}

.theme-souls .counter-unit {
  font-family: 'Cinzel', serif;
  color: var(--counter-text);
  opacity: 0.85;
}

/* ============================================================
   THEME 4: Cute Pastel
   ============================================================ */
.theme-cute-pastel {
  border-radius: 28px;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.15) 0%, rgba(255, 255, 255, 0.02) 100%), var(--counter-bg);
  border: 2px solid var(--counter-border);
  box-shadow: 0 10px 24px rgba(0, 0, 0, 0.15), 0 0 16px var(--counter-glow);
}

.theme-cute-pastel .counter-title {
  font-family: 'Quicksand', sans-serif;
  color: var(--counter-text);
  font-weight: 800;
}

.theme-cute-pastel .counter-icon-box {
  background: rgba(255, 255, 255, 0.15);
  border-color: var(--counter-border);
  border-radius: 20px;
  color: var(--counter-accent);
}

.theme-cute-pastel .counter-value {
  font-family: 'Quicksand', sans-serif;
  color: var(--counter-accent);
  text-shadow: 0 2px 8px var(--counter-glow);
}

.theme-cute-pastel .counter-unit {
  font-family: 'Quicksand', sans-serif;
  color: var(--counter-text);
  opacity: 0.85;
}

.theme-cute-pastel .counter-value-container {
  border-left-color: var(--counter-border);
}

/* ============================================================
   THEME 5: Retro Arcade (8-Bit)
   ============================================================ */
.theme-arcade {
  border-radius: 0;
  background: var(--counter-bg);
  border: 4px solid var(--counter-border);
  box-shadow: 6px 6px 0px var(--counter-border);
  image-rendering: pixelated;
}

.theme-arcade .counter-title {
  font-family: 'Press Start 2P', monospace;
  font-size: 0.72rem;
  line-height: 1.4;
  color: var(--counter-text);
}

.theme-arcade .counter-icon-box {
  border-radius: 0;
  border: 2px solid var(--counter-border);
  background: rgba(255, 255, 255, 0.05);
  color: var(--counter-accent);
}

.theme-arcade .counter-value {
  font-family: 'Press Start 2P', monospace;
  font-size: 1.7rem;
  color: var(--counter-accent);
  text-shadow: 3px 3px 0px rgba(0, 0, 0, 0.75);
}

.theme-arcade .counter-unit {
  font-family: 'Press Start 2P', monospace;
  font-size: 0.62rem;
  color: var(--counter-text);
  opacity: 0.85;
}

.theme-arcade .counter-value-container {
  border-left: 3px solid var(--counter-border);
}
